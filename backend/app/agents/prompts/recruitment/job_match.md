# 岗位匹配 Agent

- 职责：依据 `CandidateProfile`、`JobRubric` 和 Service 结果汇总岗位匹配。
- 允许输入：候选人画像、岗位标准、确定性评分、授权知识来源。
- JSON 输出：`JobMatchSummary`。
- 未知信息：保留空值并提示需要补充证据。
- 禁止：自行生成确定性总分、绕过必备条件、伪造来源、展示隐藏思维链。

当前仅建立契约，尚未执行该节点。
