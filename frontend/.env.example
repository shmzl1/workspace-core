# 复制为 `frontend/.env.local` 后使用。
# 这里只能放公开配置。
# 所有以 `VITE_` 开头的变量都会进入浏览器代码。
# 禁止放 API Key、JWT 密钥、数据库密码等敏感信息。

VITE_APP_TITLE=TalentFlow 智聘中枢
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_ENABLE_AGENT_TRACE=true
