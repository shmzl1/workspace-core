[RAG 分块异常与极端输入](/C:/code/workspace-core/backend/tests/unit/rag/test_splitter_unit.py)
chunk_size=0、负数
负 overlap、overlap 等于/大于 chunk size
空白文档
CRLF 换行
超长文本滑动分块
连续 Markdown 标题、Unicode 中文
稳定 chunk_id、ordinal、元数据保留

[考勤规则边界](/C:/code/workspace-core/backend/tests/unit/modules/test_attendance_rules_unit.py)
09:00 准点
宽限期边界与超出一分钟
凌晨 00:00、23:59
非工作日
已迟到后早退、超过下班时间

[权限异常输入](/C:/code/workspace-core/backend/tests/unit/modules/test_auth_permissions_unit.py)
None、字符串、整数、字典、对象
空列表、重复值、空白权限
tuple / set 输入

[Guardrail 异常边界](/C:/code/workspace-core/backend/tests/unit/agents/test_guardrails_unit.py)
禁止模块及真实子模块抛出 ValueError
同前缀合法模块、空字符串正常通过

[RunStore 异常路径](/C:/code/workspace-core/backend/tests/unit/agents/test_run_store_unit.py)
不存在 Run、越权访问
容量上限
事件数量上限
终止事件关闭订阅
取消不存在订阅
超时清理与深拷贝隔离


原实现：
workspace-core/backend/app/agents/shared/guardrails.py
```python
if import_path.startswith(FORBIDDEN_DIRECT_IMPORTS):
    raise ValueError(...)
```

`str.startswith(tuple)` 只比较字符串前缀，不识别 Python 模块的点号边界，因此以下合法模块也会被误判：

```text
app.core.database_helpers
app.human_only_adapter
app.modules.recruitment.repository_cache
app.modules.interview.repository_v2
```

测试结果：
![alt text](边界异常.png)
```text
4 failed, 12 passed
```

修复后只拦截禁用模块本身及其真正的子模块：

```python
if any(
    import_path == forbidden or import_path.startswith(f"{forbidden}.")
    for forbidden in FORBIDDEN_DIRECT_IMPORTS
):
    raise ValueError(...)
```

回归结果：
![alt text](边界异常处理.png)
```text
16 passed
```