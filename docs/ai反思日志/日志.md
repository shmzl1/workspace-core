# AI反思日志

# 2026年7月6日

## 一：项目范围与整体架构确定

### 问了什么

我们向 AI 询问：企业人力资源管理 Agent 项目应如何确定最终范围，如何在较短开发周期内兼顾功能完整度、技术深度和演示效果。

### AI 给了什么

AI建议将项目划分为以下主要模块：

1. 招聘管理
2. 员工服务
3. 考勤管理
4. 薪资预审
5. 权限审计
6. Agent 工作流
7. 企业制度 RAG
8. Web 管理端
9. 微信小程序员工端
10. Gradio 内部调试台

AI还提出使用一套 FastAPI 后端，同时支撑多个前端入口，并采用分层架构：

`API → Service → Repository → PostgreSQL`

Agent功能通过Tool调用业务层：

`Agent → Tool → Service`

### 我们实际改了什么

1. 最终确定项目范围为招聘、员工服务、考勤、薪资预审、权限审计、Agent、RAG、Web和小程序。
2. 确定使用一套FastAPI后端，Web、小程序和Gradio共用后端接口。
3. 普通业务请求采用API、Service、Repository和PostgreSQL分层。
4. Agent不直接访问数据库，而是通过Tool调用Service。
5. 将Gradio定位为内部调试工具，不作为正式业务入口。
6. 根据项目周期删除过于庞大、短期无法完成的功能。

## 二：开发环境与分支规范

### 问了什么

我们向AI询问：四人团队如何统一开发环境、数据库端口、Git分支和前后端启动方式，避免每个人本地配置不同导致联调失败。

### AI 给了什么

AI建议：

1. 统一使用`dev`分支进行日常开发。
2. 稳定版本再合并到`main`。
3. 使用Docker启动PostgreSQL。
4. 使用Alembic管理数据库迁移。
5. 前端和后端分别维护环境变量示例。
6. 将数据库、后端和前端分开启动。
7. 在README中记录环境配置和常用命令。

### 我们实际改了什么

1. 日常开发统一使用`dev`分支。
2. `main`作为稳定分支，不直接进行日常修改。
3. PostgreSQL通过Docker Compose启动。
4. 数据库结构使用Alembic维护。
5. 后端统一使用FastAPI，前端统一使用Vue。
6. 明确数据库、后端和前端的启动顺序。
7. 修正数据库端口和后端环境变量不一致的问题。

# 2026年7月7日

## 功能点一：团队模块分工与系统边界

### 问了什么

我们向AI询问：四名成员如何划分招聘、考勤、薪资、权限和Agent模块，才能减少代码冲突，并保证每个人都有可以独立讲解的内容。

### AI 给了什么

AI建议按照业务模块进行分工，并为每个模块规定：

1. API入口
2. Service业务层
3. Repository数据层
4. 数据模型
5. 前端页面
6. 测试范围

AI还建议把高风险算法单独隔离，避免普通业务代码和核心规则混合。

### 我们实际改了什么

1. 按招聘、员工服务、考勤、薪资和权限审计划分业务模块。
2. 招聘评分和面试排期由黄钧负责。
3. 薪资权限校验由吴越负责。
4. 前端模块按HR端和员工端继续拆分。
5. 核心算法放入`human_only`目录。
6. 普通API不得直接编写数据库查询。
7. Agent不得直接访问Repository或核心算法文件。

---

## 功能点二：数据库结构设计

### 问了什么

我们向AI询问：项目需要哪些数据库表，如何覆盖员工、招聘、面试、考勤、薪资、权限、审计和Agent运行记录。

### AI 给了什么

AI根据功能范围给出了用户、员工、岗位、候选人、申请、面试、考勤、薪资、制度、审计和通知等实体建议，并建议：

1. 使用外键表达业务关系。
2. 将数据库迁移和演示数据分离。
3. 权限以稳定字符串代码保存。
4. 重要业务表保留创建时间和更新时间。
5. 招聘评分和排期结果需要保存关键输入快照。

### 我们实际改了什么

1. 设计并建立覆盖项目主要业务的数据库表。
2. 将用户账号和员工档案分开。
3. 建立岗位、候选人和候选人申请之间的关系。
4. 建立面试官、会议室和面试记录相关表。
5. 建立考勤、假期、薪资和权限审计数据结构。
6. 数据库迁移只负责表结构，演示数据通过独立脚本导入。
7. 评分结果保存评分明细和权重快照，避免后续权重变化影响历史结果。

# 2026年7月8日

## 功能点一：候选人池评分实时化

### 问了什么

我们向AI询问：为什么候选人池中的候选人评分基本固定为81%，为什么部分候选人提示无法获取智能评估，如何让评分随数据库数据变化。

### AI 给了什么

AI建议检查完整调用链：

1. 前端是否还在使用固定候选人数据。
2. API是否真正调用招聘Service。
3. Service是否从PostgreSQL读取候选人和岗位。
4. 是否调用评分算法。
5. 评分结果是否保存到候选人申请记录。
6. 前端字段是否与后端返回字段一致。

AI还建议将评分拆分为技能、经验、项目、学历和风险等维度。

### 我们实际改了什么

1. 删除候选人池中的固定评分展示逻辑。
2. 候选人、岗位和申请数据改为从PostgreSQL读取。
3. 统一评分接口和前端字段。
4. 保存总分、匹配度、风险标签、评分依据和各维度贡献。
5. 候选人池从数据库中的实际评分记录读取数据。
6. 评分算法不可用时展示明确错误，不再用固定分数掩盖失败。

---

## 功能点二：面试日历与智能排期联调

### 问了什么

我们向AI询问：面试日历为什么一直处于加载状态，为什么智能排期不能返回结果，以及候选人、面试官和会议室应该如何参与排期。

### AI 给了什么

AI建议检查：

1. 前端传递的是候选人编号还是申请编号。
2. 后端是否从数据库读取可用时间。
3. 候选人、面试官和会议室是否存在共同时间段。
4. 是否读取已有面试并检查冲突。
5. 预览结果和确认结果是否使用相同字段。
6. 后端不可用时是否有明确错误提示。

### 我们实际改了什么

1. 统一前端排期入口使用`application_id`。
2. 后端根据申请记录查询候选人和岗位。
3. 从数据库读取候选人、面试官、会议室和已有面试。
4. 由排期算法计算三方可用时间交集。
5. 排除候选人、面试官和会议室的冲突时间。
6. 排期确认前再次检查冲突。
7. 修复面试日历加载状态无法结束的问题。

---

## 功能点三：招聘报告实时化

### 问了什么

我们向AI询问：招聘报告如何根据岗位、候选人、申请阶段和评分的变化自动更新，而不是展示固定统计数据。

### AI 给了什么

AI建议由后端统一汇总：

1. 招聘岗位数量
2. 候选人数量
3. 招聘阶段分布
4. 部门招聘情况
5. 候选人来源
6. 月度变化趋势
7. 平均招聘评分

AI建议前端只负责展示后端结果，不再维护独立的固定统计数据。

### 我们实际改了什么

1. 增加招聘报告数据汇总接口。
2. 报告从岗位、候选人和申请记录实时计算。
3. 招聘报告与候选人池读取同一份评分记录。
4. 支持不同时间范围查询。
5. 空数据库返回零值和空数组。
6. 删除固定招聘统计数据。

---

# 2026年7月9日

## 功能点一：候选人评估失败问题排查

### 问了什么

我们向AI询问：后端已经存在招聘评分代码，但前端仍然显示“无法获取智能评估”，应从哪些位置排查。

### AI 给了什么

AI建议依次检查：

1. 浏览器网络请求是否成功。
2. 请求路径和API前缀是否一致。
3. JWT和权限是否正确。
4. 后端响应是否符合统一响应格式。
5. 评分字段是否位于`data`内部。
6. 候选人编号、申请编号和岗位编号是否混用。
7. 异常是否被前端统一转换成网络错误。

### 我们实际改了什么

1. 对齐前端API地址和后端路由。
2. 修正候选人编号与申请编号的使用。
3. 统一后端响应中的评分字段。
4. 修正前端读取`overall_score`、`match_score`和评分明细的位置。
5. 分开处理401、403、业务错误和网络错误。
6. 前端不再把所有异常都显示为“无法连接网络”。

## 功能点二：前端业务状态统一

### 问了什么

我们向AI询问：候选人池、面试日历、招聘报告、薪资和考勤页面如何统一处理加载、空数据、权限不足和后端错误状态。

### AI 给了什么

AI建议为页面统一设计：

1. Loading状态
2. Empty状态
3. Error状态
4. PermissionDenied状态
5. 正常业务状态
6. 重试操作
7. Toast提示

### 我们实际改了什么

1. 为主要页面增加加载状态。
2. 无业务数据时显示空数据提示。
3. 403时显示权限拒绝组件。
4. 网络和后端错误显示具体错误信息。
5. 页面提供重新加载入口。
6. 删除后端失败后自动显示固定业务数据的做法。
7. 新功能嵌入原有页面，不显示“Sprint 1”“开发中”等过程性文字。

---

# 2026年7月10日

## 功能点一：招聘评分算法代码理解与边界检查

### 问了什么

我们向AI询问：招聘评分算法中每个函数的作用是什么，技能、经验、项目、学历和风险分数如何参与总分计算。

### AI 给了什么

AI逐步解释了：

1. 输入数据读取。
2. 技能列表标准化。
3. 经验年限计算。
4. 项目关键词匹配。
5. 学历分数计算。
6. 风险标签生成。
7. 权重归一化。
8. 加权总分和推荐动作生成。

AI还指出应检查缺失字段、非法权重和异常数据类型。

### 我们实际改了什么

1. 重新检查评分算法的每个辅助函数。
2. 确认权重总和异常时进行归一化。
3. 对空候选人和空岗位数据提供稳定返回。
4. 保留评分依据和加权贡献，便于答辩解释。
5. 识别出必备技能和加分技能不应完全合并的问题。
6. 补充对异常`profile_json`类型的处理计划。

---

## 功能点二：面试排期算法代码理解

### 问了什么

我们向AI询问：排期算法中的时间解析、交集计算、冲突判断、候选方案评分和结果序列化分别有什么作用。

### AI 给了什么

AI解释了主要流程：

1. 读取候选人可用时间。
2. 遍历面试官可用时间。
3. 计算共同时间区间。
4. 叠加会议室可用时间。
5. 检查已有面试冲突。
6. 对候选时段评分。
7. 选择评分最高的时间。
8. 返回推荐时段和冲突说明。

### 我们实际改了什么

1. 明确排期算法输入和返回结构。
2. 检查无可用时间和缺少资源时的返回。
3. 保留最多五个候选排期方案。
4. 将排期原因和冲突说明返回前端。
5. 识别面试时长非法、零值和负值问题。
6. 识别带时区和不带时区时间可能无法比较的问题。

## 功能点三：薪资访问控制代码理解

### 问了什么

我们向AI询问：`salary_access_control.py`为什么会返回不同格式，字典参数分支和角色权限分支分别在做什么。

### AI 给了什么

AI解释：

1. 参数为角色字符串时，函数进行字段级薪资访问判断。
2. 本人权限可读取本人完整字段。
3. 全量权限可读取完整字段。
4. 部门管理权限只能读取有限字段。
5. 脱敏权限只能读取部分字段。
6. 参数为字典时，函数返回薪资预审摘要。

### 我们实际改了什么

1. 明确薪资访问判断和薪资预审是两种不同业务。
2. 检查本人、全量、部门和脱敏权限顺序。
3. 保留无权限时默认拒绝。
4. 要求所有敏感访问写入审计日志。
5. 识别同一函数返回两类结构不利于维护的问题。
6. 后续计划将薪资访问判断和薪资预审拆分为两个函数。

# 2026年7月11日

## 功能点一：多Agent招聘架构重构

### 问了什么

我们向AI询问：如何将招聘评分、简历解析、岗位匹配、面试信息、决策审查和招聘报告组织成真正可解释的多Agent工作流。

### AI 给了什么

AI建议建立以下节点：

1. 招聘策略Agent
2. 简历解析Agent
3. 岗位匹配Agent
4. 面试评估Agent
5. 决策审查Agent
6. HR最终报告节点

AI还建议记录每个节点的输入、输出、状态、耗时和来源。

### 我们实际改了什么

1. 重构招聘多Agent流程。
2. 招聘策略节点生成结构化执行计划。
3. 简历解析节点只提取已有事实，不虚构简历信息。
4. 岗位匹配节点调用确定性评分Service。
5. 没有真实面试评价时，面试评估节点标记为`SKIPPED`。
6. 决策审查节点检查评分、证据、风险和可信度。
7. HR报告节点只汇总结果，不直接决定录用或淘汰。
8. 最终决定保留给HR人工完成。

---

## 功能点二：Agent调用边界

### 问了什么

我们向AI询问：Agent如何调用招聘评分和业务数据，同时保证不会绕过Service、权限和核心算法边界。

### AI 给了什么

AI建议使用Tool封装业务能力，并通过依赖注入向Runner提供Tool和模型网关。

### 我们实际改了什么

1. 确定Agent调用链为：

   `Agent → Tool → Service → Repository`
2. 涉及核心算法时采用：

   `Agent → Tool → Service → human_only`
3. Agent不得直接访问Repository。
4. Agent不得直接导入`human_only`算法。
5. Tool只负责调用Service，不复制业务算法。
6. API仍然通过Service处理普通业务请求。

## 功能点三：Agent运行状态与SSE实时展示

### 问了什么

我们向AI询问：如何让前端实时看到Agent执行过程，并在页面刷新后恢复之前的运行状态。

### AI 给了什么

AI建议：

1. 每次运行生成`run_id`。
2. 每次请求生成`trace_id`。
3. 使用SSE推送节点和Tool事件。
4. 保存Run Snapshot。
5. 页面刷新后通过GET接口恢复。
6. 事件增加唯一ID并进行去重。

### 我们实际改了什么

1. 建立Agent Run、节点、事件和Tool调用数据结构。
2. Run状态保存到PostgreSQL。
3. SSE用于推送真实Agent事件。
4. 历史事件从数据库重新读取。
5. 页面刷新后通过Run Snapshot恢复状态。
6. 不同用户不能读取对方创建的Run。
7. 事件只返回安全错误信息，不暴露完整异常和敏感输入。

---

# 2026年7月13日

## 功能点一：LLM与RAG接入及回退机制

### 问了什么

我们向AI询问：如何为招聘策略和HR报告接入大模型与企业知识检索，同时保证模型或向量数据库不可用时系统仍能运行。

### AI 给了什么

AI建议增加：

1. 模型网关
2. Embedding客户端
3. 文档加载器
4. 文本切分器
5. ChromaDB向量存储
6. 知识检索网关
7. 模型调用超时
8. 本地规则回退
9. 健康检查状态

### 我们实际改了什么

1. LLM和RAG作为可选集成。
2. 未配置模型时不阻塞普通业务启动。
3. 策略和报告先生成确定性结果，再尝试模型增强。
4. 模型失败时使用`RULE_BASED_FALLBACK`。
5. RAG不可用时使用`LOCAL_HYBRID_FALLBACK`。
6. 前端明确显示真实集成状态。
7. 不把本地回退描述为真实ChromaDB命中。
8. 健康检查隐藏密钥、数据库连接串和本地绝对路径。

## 功能点二：Agent前端实时执行面板

### 问了什么

我们向AI询问：前端如何展示执行计划、Agent节点、候选人进度、岗位匹配、审查结果和最终报告。

### AI 给了什么

AI建议拆分：

1. 招聘目标表单
2. 工作流看板
3. Agent事件流
4. 节点详情
5. 候选人结果面板
6. 决策审查面板
7. HR报告面板
8. 集成状态面板

### 我们实际改了什么

1. 将多Agent评估页面拆分为多个组件。
2. 创建Run后将`run_id`写入URL。
3. 根据SSE事件实时更新节点和候选人状态。
4. 根据事件ID进行去重。
5. 页面刷新后读取Snapshot恢复运行。
6. 展示`COMPLETED`、`NEEDS_REVIEW`、`SKIPPED`和`FAILED`状态。
7. 删除随机日志、固定延迟和虚假Agent事件。

## 功能点三：质量门禁与边界场景整理

### 问了什么

我们向AI询问：根据Sprint 2验收要求，应为核心模块准备哪些单元测试和边界场景。

### AI 给了什么

AI建议重点覆盖：

1. 正常输入
2. 空输入
3. 字段缺失
4. 类型错误
5. 极端数值
6. 权限不足
7. 数据不存在
8. 网络中断
9. 并发请求
10. 服务降级

### 我们实际改了什么

1. 整理招聘评分边界场景。
2. 整理面试排期冲突场景。
3. 整理Agent重复审批和Run所有者隔离场景。
4. 整理SSE断流和页面恢复场景。
5. 整理登录失效和权限拒绝场景。
6. 整理重复签到、未签到直接签退等考勤场景。
7. 将问题分为真实缺陷、潜在风险和一般优化。

# 2026年7月14日

## 功能点一：组内交叉代码Review

### 问了什么

我们向AI询问：如何根据`dev`分支实际代码，检查四名成员负责模块中的业务逻辑、安全、并发和可维护性问题。

### AI 给了什么

AI辅助整理出以下检查方向：

1. 招聘评分中必备技能和加分技能是否分离。
2. 异常候选人数据是否会导致评分崩溃。
3. 面试时长和时间格式是否合法。
4. Agent审批是否存在并发问题。
5. Agent运行锁是否及时清理。
6. SSE单个事件错误是否会中断连接。
7. 待人工复核状态是否前后端一致。
8. 网络错误是否会被错误处理为登录失效。
9. 考勤权限代码是否与业务一致。

### 我们实际改了什么

1. A和B于7月13日完成交叉Review。
2. C和D于7月14日完成交叉Review。
3. 每条Review意见均定位到具体文件或函数。
4. 删除无法对应实际代码的泛化问题。
5. 区分高优先级缺陷、中等风险和一般优化。
6. 为需要修改的问题安排原审查人复审。
7. 保留Review记录作为验收材料。

## 功能点二：验收材料与AI反思日志整理

### 问了什么

我们向AI询问：如何严格按照验收要求整理AI使用记录，使每个功能点都包含“问了什么、AI给了什么、我们改了什么”。

### AI 给了什么

AI最初给出的内容偏向一般项目总结，没有严格按照日期和功能点展开，部分内容也缺少明确的实际修改说明。

### 我们实际改了什么

1. 按2026年7月6日至7月14日的日期顺序重新整理。
2. 按要求跳过7月12日。
3. 每个日期继续按具体功能点拆分。
4. 每个功能点都记录：
   - 问了什么
   - AI给了什么
   - 我们实际改了什么
   - 使用反思
5. 删除空泛的AI使用描述。
6. 不把AI建议写成已经完成或已经测试通过的事实。
7. 明确最终代码、业务规则和验收责任属于团队成员。
