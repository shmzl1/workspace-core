# 本文件是本地开发和 Docker Compose 的示例配置。
# 请复制为根目录 `.env` 后，再按本机情况修改。
# 根目录 `.env` 不得提交到仓库。
# 不要在这里填写真实模型 API Key、密码或 Token。

POSTGRES_DB=talentflow
POSTGRES_USER=talentflow
POSTGRES_PASSWORD=talentflow_dev_password_change_me
POSTGRES_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=5173
